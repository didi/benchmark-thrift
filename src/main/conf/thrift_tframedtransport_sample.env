# Rename filename to `thrift.env` if you want to use this sample

# 1. thrift version
# available: 0.9.0, 0.9.1, 0.9.2, 0.9.3, 0.10.0, 0.11.0, 0.12.0
thrift_version=0.11.0

# 2. jar
# could be an absolute path, or a path relative to this env file
client_jar=/Users/didi/IdeaProjects/benchmarkthrift/demo-thrift-server/target/demo-thrift-server-1.0-SNAPSHOT.jar

# 3. transport
transport=TFramedTransport(transport=TSocket)
#transport=TFramedTransport(transport=TSocket(timeout=100))
#transport=TFramedTransport(transport=TSocket(timeout=100), maxLength=1000000000)

# 4. protocol
# available: TBinaryProtocol, TCompactProtocol, TSimpleJSONProtocol, TJSONProtocol
protocol=TBinaryProtocol
