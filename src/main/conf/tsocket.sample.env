# Rename filename to `thrift.env` if you want to use this sample

# 1. thrift version
# the following versions are supported: 0.9.0, 0.9.1, 0.9.2, 0.9.3, 0.10.0, 0.11.0, 0.12.0
version=0.11.0

# 2. jar
classpath=/Users/didi/IdeaProjects/benchmarkthrift/thrift-benchmark/src/test/resources/base-1.0-SNAPSHOT.jar

# 3. transport
transport=TSocket
#transport=TSocket(timeout=100)
#transport=TSocket(socketTimeout=100,connectTimeout=10)

## 示例2(Demo 2) : TFramedTransport
## 如果不指定TFramedTransport内部的TTransport,默认为TSocket
#transport=TFramedTransport
transport=TFramedTransport(transport=TSocket)
#transport=TFramedTransport(transport=TSocket(timeout=100))
#transport=TFramedTransport(transport=TSocket(timeout=100), maxLength=1000000000)

# 4. protocol
# the following protocols are supported: TBinaryProtocol, TCompactProtocol, TSimpleJSONProtocol, TJSONProtocol
protocol=TBinaryProtocol